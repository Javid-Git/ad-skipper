# The accessibility service is instantiated by the system from the manifest
# entry, so keep it even if minification is turned on later.
-keep class dev.javid.adskipper.SkipAdAccessibilityService { *; }
