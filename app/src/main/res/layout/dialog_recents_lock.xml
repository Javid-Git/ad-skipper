<?xml version="1.0" encoding="utf-8"?><!--
    Body of the "how do I lock it in Recents" dialog. The frame, title and
    dismiss button come from the platform AlertDialog; only this content is
    ours, so there is no dependency on a dialog library.
-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:scrollbars="none">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingTop="8dp"
        android:paddingEnd="24dp"
        android:paddingBottom="8dp">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="180dp"
            android:adjustViewBounds="true"
            android:contentDescription="@string/recents_lock_image_description"
            android:src="@drawable/illus_recents_lock" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:lineSpacingExtra="4dp"
            android:paddingTop="12dp"
            android:text="@string/recents_lock_steps"
            android:textColor="@color/text_primary"
            android:textSize="15sp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:lineSpacingExtra="3dp"
            android:paddingTop="14dp"
            android:text="@string/recents_lock_caveat"
            android:textColor="@color/text_secondary"
            android:textSize="13sp"
            android:textStyle="italic" />
    </LinearLayout>

</ScrollView>
