<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault.DayNight">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            exported="true" is required: the binder is system_server, which lives
            outside this app. It is gated by BIND_ACCESSIBILITY_SERVICE, a
            signature-level permission only the system holds, so nothing else on
            the device can bind to this service.
        -->
        <!--
            Keep the accessibility service independent from the launcher
            activity. Removing the app's task from Recents must not tear down
            the service or make Android treat the app as no longer running.
            (A user-invoked Force stop is intentionally still terminal and
            cannot be bypassed by an ordinary app.)
        -->
        <service
            android:name=".SkipAdAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:process=":accessibility"
            android:stopWithTask="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
